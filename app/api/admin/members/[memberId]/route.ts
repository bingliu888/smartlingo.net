import { isAdmin, isBootstrapAdminEmail } from "../../../../../lib/admin-access";
import { getDatabase, getSessionUser } from "../../../../../lib/auth";

type Target={id:string;email:string;role:"member"|"admin"};
export async function PATCH(request:Request,{params}:{params:Promise<{memberId:string}>}){const admin=await getSessionUser(request);if(!admin)return Response.json({error:"Authentication required"},{status:401});if(!isAdmin(admin))return Response.json({error:"Administrator access required"},{status:403});const{memberId}=await params;const target=await getDatabase().prepare("SELECT id,email,role FROM users WHERE id=? LIMIT 1").bind(memberId).first<Target>();if(!target)return Response.json({error:"Member not found"},{status:404});const body=await request.json().catch(()=>({})) as {role?:string};if(body.role!=="member"&&body.role!=="admin")return Response.json({error:"Role must be member or admin"},{status:400});if((isBootstrapAdminEmail(target.email)||target.id===admin.id)&&body.role!=="admin")return Response.json({error:"This administrator role is protected"},{status:409});await getDatabase().prepare("UPDATE users SET role=? WHERE id=?").bind(body.role,target.id).run();return Response.json({ok:true,member:{id:target.id,role:body.role}})}
