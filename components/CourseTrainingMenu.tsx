"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import type { SmartLingoPackageTier } from "../lib/smartlingo-course-packages";

export function CourseTrainingMenu({ lang, classId }: { lang: "zh" | "en"; classId: string }) {
  const zh = lang === "zh";
  const [tier, setTier] = useState<SmartLingoPackageTier>("basic");
  const [sprintOpen, setSprintOpen] = useState(false);
  const [sprintMinutes, setSprintMinutes] = useState<5 | 10 | 15 | 20>(10);
  useEffect(() => {
    fetch(`/api/classes/${encodeURIComponent(classId)}`, { cache: "no-store" })
      .then(response => response.ok ? response.json() : null)
      .then(data => { if (data?.class?.packageTier) setTier(data.class.packageTier); })
      .catch(() => undefined);
  }, [classId]);
  const base = `/${lang}/classes/${encodeURIComponent(classId)}/learn/session`;
  const items = [
    { key: "vocab", icon: "Aa", title: zh ? "每日词汇" : "Daily vocabulary", copy: zh ? "智慧卡 · 21 天记忆 · 掌握报告" : "SmartCards · 21-day memory · reports", href: `/${lang}/classes/${encodeURIComponent(classId)}/vocabulary` },
    { key: "reading", icon: "▤", title: zh ? "阅读" : "Reading", copy: zh ? "理解句子 · 选择与反馈" : "Sentence meaning · choices · feedback", href: `${base}?training=reading` },
    { key: "speaking", icon: "◉", title: zh ? "口语" : "Speaking", copy: zh ? "跟我说 · 回答我 · 即时纠正" : "Repeat · answer · instant correction", href: `${base}?training=dialogue` },
    { key: "listening", icon: "◒", title: zh ? "听力" : "Listening", copy: zh ? "听辨语音 · 理解语境" : "Sound recognition · context", href: `${base}?training=listening` },
    { key: "writing", icon: "✎", title: zh ? "写作" : "Writing", copy: zh ? "组织表达 · 修改建议" : "Structured writing · guided revision", href: `${base}?training=writing` },
    { key: "quiz", icon: "?", title: zh ? "测验" : "Quiz", copy: zh ? "词汇与情境题 · 即时判分" : "Vocabulary and scenarios · instant scoring", href: `${base}?training=quiz` },
    ...(tier === "advanced" ? [
      { key: "accent", icon: "◎", title: zh ? "口音校正" : "Accent correction", copy: zh ? "跟读比较 · 发音纠正" : "Speak and compare · correction", href: `${base}?training=dialogue&focus=accent` },
      { key: "speech", icon: "▰", title: zh ? "演讲训练" : "Speech training", copy: zh ? "演讲表达 · 人工智能反馈" : "Delivery practice · AI feedback", href: `${base}?training=dialogue&focus=speech` },
      { key: "draft", icon: "¶", title: zh ? "演讲稿修改" : "Speech-draft revision", copy: zh ? "结构、用词与表达润色" : "Structure, wording, and polish", href: `${base}?training=writing&focus=speech-draft` },
    ] : []),
  ];
  return <section className="course-training-menu" aria-labelledby="course-training-title">
    <header><p>{zh ? "每日练习" : "DAILY PRACTICE"}</p><h1 id="course-training-title">{zh ? "选择训练方式" : "Choose your training"}</h1><span>{zh ? "课程等级决定可用训练内容。" : "Your course level determines the available training."}</span></header>
    <div><button className="course-sprint-entry" type="button" onClick={() => setSprintOpen(true)}><i aria-hidden="true">⚡</i><strong>{zh ? "今日速成" : "Today’s Sprint"}</strong><small>{zh ? "5–20 分钟 · 五技能混合回合" : "5–20 minutes · mixed five-skill rounds"}</small><b>{zh ? "选择时长" : "Choose time"} →</b></button>{items.map(item => <Link href={item.href} key={item.key}><i aria-hidden="true">{item.icon}</i><strong>{item.title}</strong><small>{item.copy}</small><b>{zh ? "开始训练" : "Start training"} →</b></Link>)}</div>
    {sprintOpen ? <div className="sprint-picker-backdrop" role="presentation" onMouseDown={event => { if (event.target === event.currentTarget) setSprintOpen(false); }}><section className="sprint-picker" role="dialog" aria-modal="true" aria-labelledby="sprint-picker-title"><button className="sprint-picker-close" type="button" aria-label={zh ? "关闭" : "Close"} onClick={() => setSprintOpen(false)}>×</button><p>DAILY SPRINT</p><h2 id="sprint-picker-title">{zh ? "今天想学多久？" : "How long do you want to learn?"}</h2><span>{zh ? "每 5 分钟是一个完整五技能回合；默认 10 分钟。" : "Every five minutes is one complete five-skill round. Ten minutes is the default."}</span><div>{([5,10,15,20] as const).map(minutes => <button type="button" className={sprintMinutes === minutes ? "selected" : ""} aria-pressed={sprintMinutes === minutes} onClick={() => setSprintMinutes(minutes)} key={minutes}><strong>{minutes} {zh ? "分钟" : "min"}</strong><small>{minutes/5} {zh ? "回合" : minutes === 5 ? "round" : "rounds"} · {minutes*2} {zh ? "个新词" : "new words"}</small><em>{zh ? "词汇跟读、阅读、听力组句、写作组句、口语" : "Vocabulary repeat, reading, listening, writing, speaking"}</em></button>)}</div><Link className="sprint-picker-start" href={`/${lang}/classes/${encodeURIComponent(classId)}/sprint?minutes=${sprintMinutes}`}>{zh ? `开始 ${sprintMinutes} 分钟速成` : `Start ${sprintMinutes}-minute sprint`} →</Link></section></div> : null}
    <style>{`.course-training-menu{width:min(1200px,calc(100% - 40px));margin:44px auto 18px;padding:28px;border-radius:24px;background:#123f35;color:#fff}.course-training-menu header p{margin:0;color:#63d4b0;font-size:12px;font-weight:900;letter-spacing:.12em}.course-training-menu h1{margin:10px 0;font-size:clamp(28px,3.5vw,44px)}.course-training-menu header span{color:#c5d8d1}.course-training-menu>div:first-of-type{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px;margin-top:24px}.course-training-menu a,.course-training-menu .course-sprint-entry{min-width:0;padding:20px;display:flex;flex-direction:column;border:1px solid rgba(255,255,255,.18);border-radius:18px;background:rgba(255,255,255,.06);color:#fff;text-align:left;text-decoration:none}.course-training-menu .course-sprint-entry{font:inherit;cursor:pointer;background:linear-gradient(145deg,#b9ef4d,#78ad2d);color:#153129}.course-training-menu i{width:48px;height:48px;display:grid;place-items:center;border-radius:13px;background:#62dab5;color:#123f35;font-style:normal;font-weight:900}.course-training-menu strong{margin-top:18px;font-size:24px}.course-training-menu small{margin:8px 0 20px;color:#bfd1cb;line-height:1.5}.course-training-menu .course-sprint-entry small{color:#294b2c}.course-training-menu b{margin-top:auto;color:#67dbb8;font-size:13px}.course-training-menu .course-sprint-entry b{color:#173c25}.sprint-picker-backdrop{position:fixed;z-index:1000;inset:0;margin:0!important;padding:18px;display:grid!important;place-items:center;background:#081c18b8}.sprint-picker{position:relative;width:min(760px,100%);max-height:calc(100vh - 36px);padding:clamp(24px,5vw,44px);overflow:auto;border-radius:25px;background:#f7f3ea;color:#17342c}.sprint-picker>p{color:#087d62;font-size:12px;font-weight:950;letter-spacing:.12em}.sprint-picker h2{margin:8px 0;font-size:clamp(34px,5vw,54px)}.sprint-picker>span{color:#5f716a}.sprint-picker-close{position:absolute;right:18px;top:18px;width:44px;height:44px;border:0;border-radius:12px;background:#123f35;color:#fff;font-size:26px}.sprint-picker>div{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:25px 0}.sprint-picker>div button{min-width:0;padding:16px;display:grid;text-align:left;border:1px solid #b9cbc3;border-radius:16px;background:#fff;color:#17342c}.sprint-picker>div button.selected{border:2px solid #087d62;background:#e3f6ee}.sprint-picker>div button strong{margin:0}.sprint-picker>div button small{margin:5px 0;color:#087d62}.sprint-picker>div button em{font-style:normal;color:#61726c}.sprint-picker-start{display:flex!important;align-items:center!important;justify-content:center!important;min-height:54px;background:#087d62!important;color:#fff!important;text-align:center!important}@media(max-width:820px){.course-training-menu>div:first-of-type{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:560px){.course-training-menu{width:calc(100% - 28px);padding:18px}.course-training-menu>div:first-of-type,.sprint-picker>div{grid-template-columns:1fr}}`}</style>
  </section>;
}
